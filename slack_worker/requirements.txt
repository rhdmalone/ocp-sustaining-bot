# Slack Worker Service Requirements
# Base dependencies inherited from root requirements.txt:
#   - gspread, httpx, python-dotenv, dynaconf[vault], pytest, slack-sdk

# Service-specific packages

# APScheduler for job scheduling
APScheduler==3.10.4

# Smartsheet SDK for reading data
smartsheet-python-sdk==3.0.3

# Timezone support
pytz==2024.1

# Testing extensions (pytest is in root)
pytest-cov==6.0.0
pytest-mock==3.14.0


